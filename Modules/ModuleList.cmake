### Change the prefix that gets added automatically to DeepMRSegApp
# set(MITK_MODULE_NAME_PREFIX "DeepMRSegApp")

### Attention: If a module depends on another, it should be after it!

set(MITK_MODULES
	CbicaDeepMRSeg
)
